<div class="mb-6 md:mb-8 flex flex-col md:flex-row md:items-end justify-between gap-4">
  <div>
    <h2 class="text-2xl md:text-3xl font-medium text-aprende-text">Alunos Disponíveis</h2>
    <p class="text-aprende-muted mt-2">Conheça os alunos que buscam aprendizado na plataforma.</p>
  </div>

  <div class="w-full md:w-72 relative">
    <%= form_with url: students_path, method: :get, local: true, class: "group" do |f| %>
      <div class="relative flex items-center">
        <div class="absolute left-4 text-aprende-muted group-focus-within:text-aprende-primary transition-colors">
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 4a1 1 0 011-1h16a1 1 0 011 1v2.586a1 1 0 01-.293.707l-6.414 6.414a1 1 0 00-.293.707V17l-4 4v-6.586a1 1 0 00-.293-.707L3.293 7.293A1 1 0 013 6.586V4z"></path></svg>
        </div>
        <%= f.select :subject_id, Subject.all.order(:name).map { |s| [s.name, s.id] }, { include_blank: "Todas as Áreas", selected: params[:subject_id] }, { onchange: "this.form.submit()", class: "w-full pl-11 pr-4 py-3 bg-white border border-aprende-secondary rounded-full text-sm text-aprende-text focus:outline-none focus:ring-4 focus:ring-aprende-primary/10 focus:border-aprende-primary appearance-none transition-all shadow-sm cursor-pointer hover:border-aprende-primary/50" } %>
        <div class="absolute right-4 pointer-events-none text-aprende-muted">
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path></svg>
        </div>
      </div>
    <% end %>
  </div>
</div>

<% if @students.any? %>
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4 md:gap-6">
    <% @students.each do |student| %>
      <div class="bg-white rounded-[1.5rem] shadow-sm border border-aprende-secondary p-6 md:p-8 flex flex-col h-full gap-4">
        <div class="flex-1 flex flex-col gap-4">
          <div class="flex items-center gap-3">
            <%= user_avatar(student, size: "w-14 h-14", text_size: "text-xl") %>
            <div>
              <h3 class="text-xl font-medium text-aprende-text"><%= student.name %></h3>
            </div>
          </div>
          
          <% if student.subjects.any? %>
            <div class="mt-4">
              <p class="text-sm text-aprende-muted mb-3 font-medium">Áreas de Interesse</p>
              <div class="flex flex-wrap gap-2">
                <% student.subjects.each do |subject| %>
                  <span class="bg-aprende-bg border border-aprende-secondary text-aprende-text text-xs px-3 py-1.5 rounded-full font-medium"><%= subject.name %></span>
                <% end %>
              </div>
            </div>
          <% end %>

          <div class="mt-4">
            <p class="text-sm text-aprende-muted mb-2 font-medium">Preferências e Objetivos</p>
            <p class="text-aprende-text text-xs leading-relaxed italic line-clamp-3">
              <%= user_preferences_display(student, truncated: true) %>
            </p>
          </div>

          <div class="mt-4">
            <p class="text-sm text-aprende-muted mb-2 font-medium">Disponibilidade</p>
            <p class="text-aprende-text text-xs leading-relaxed italic">
              <%= student.availability.present? ? student.availability : "Horário a combinar." %>
            </p>
          </div>
        </div>

        <div class="mt-auto pt-6 border-t border-aprende-secondary flex flex-col gap-3">
          <% if current_user&.teacher? %>
            <%= link_to "Fazer Proposta", new_proposal_path(student_id: student.id), class: "block w-full text-center bg-aprende-primary hover:bg-[#159b3f] text-white font-medium py-3 rounded-xl transition-colors" %>
          <% elsif !current_user %>
            <%= link_to "Entrar para Ver Perfil", login_path, class: "block w-full text-center bg-aprende-primary hover:bg-[#159b3f] text-white font-medium py-3 rounded-xl transition-colors" %>
          <% else %>
            <%= link_to "Ver Perfil", student_path(student), class: "block w-full text-center bg-white border border-aprende-secondary hover:bg-aprende-bg text-aprende-text font-medium py-3 rounded-xl transition-colors" %>
          <% end %>
        </div>
      </div>
    <% end %>
  </div>
<% else %>
  <div class="bg-white border border-aprende-secondary rounded-xl p-8 text-center shadow-sm">
    <p class="text-aprende-muted"><%= params[:subject_id].present? ? "Nenhum aluno encontrado para esta área." : "Nenhum aluno cadastrado ainda." %></p>
    <% if params[:subject_id].present? %>
      <%= link_to "Limpar Filtro", students_path, class: "mt-4 inline-block text-aprende-primary hover:underline text-sm font-medium" %>
    <% end %>
  </div>
<% end %>
