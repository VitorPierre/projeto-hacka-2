<div class="mb-6 md:mb-8">
  <h2 class="text-2xl md:text-3xl font-medium text-aprende-text">Alunos Disponíveis</h2>
  <p class="text-aprende-muted mt-2">Conheça os alunos que buscam aprendizado na plataforma.</p>
</div>

<% if @students.any? %>
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4 md:gap-6">
    <% @students.each do |student| %>
      <div class="bg-white rounded-[1.5rem] shadow-sm border border-aprende-secondary p-6 md:p-8 flex flex-col h-full gap-4">
        <div class="flex-1 flex flex-col gap-2">
          <div>
            <h3 class="text-xl font-medium text-aprende-text"><%= student.name %></h3>
          </div>
          
          <% if student.subjects.any? %>
            <div class="mt-4">
              <p class="text-sm text-aprende-muted mb-3 font-medium">Áreas de Interesse</p>
              <div class="flex flex-wrap gap-2">
                <% student.subjects.each do |subject| %>
                  <span class="bg-aprende-bg border border-aprende-secondary text-aprende-text text-xs px-3 py-1.5 rounded-full font-medium"><%= subject.name %></span>
                <% end %>
              </div>
            </div>
          <% end %>
        </div>

        <div class="mt-auto pt-6 border-t border-aprende-secondary flex flex-col gap-3">
          <%= link_to "Ver Perfil", student_path(student), class: "block w-full text-center bg-aprende-primary hover:bg-[#159b3f] text-white font-medium py-3 rounded-xl transition-colors" %>
        </div>
      </div>
    <% end %>
  </div>
<% else %>
  <div class="bg-white border border-aprende-secondary rounded-xl p-8 text-center shadow-sm">
    <p class="text-aprende-muted">Nenhum aluno cadastrado ainda.</p>
  </div>
<% end %>
